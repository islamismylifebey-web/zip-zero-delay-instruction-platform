// Vercel is intentionally frontend-only. This module exists only as a build-time
// alias for `cloudflare:workers`; it never supplies D1/R2 access or secrets.
export const env: Record<string, never> = Object.freeze({});
