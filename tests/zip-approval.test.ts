import test from 'node:test'; import assert from 'node:assert/strict'; import { createApprovedJob, createPendingZipDraft } from '../lib/zip/approval.ts';
test('pending ZIP draft contains no raw instruction field or raw text', () => { const raw = 'yo go wash that building'; const pending = createPendingZipDraft({ draftId: 'd1', ownerEmail: 'owner@example.com', assigneeId: 'crew-1', professionalAssignment: 'Clean the assigned exterior surface.', checklist: ['Keep people clear of the active work area.'], createdBy: 'owner@example.com', createdAt: '2026-09-06T08:00:00.000Z', expiresAt: '2026-09-06T08:15:00.000Z' }); assert.equal('rawInstruction' in pending, false); assert.equal(JSON.stringify(pending).includes(raw), false); });
test('approved job persists only reviewed assignment and checklist', () => { const pending = createPendingZipDraft({ draftId: 'd1', ownerEmail: 'owner@example.com', assigneeId: 'crew-1', professionalAssignment: 'Clean the assigned exterior surface.', checklist: ['Keep people clear of the active work area.'], createdBy: 'owner@example.com', createdAt: '2026-09-06T08:00:00.000Z', expiresAt: '2026-09-06T08:15:00.000Z' }); const job = createApprovedJob(pending, 'Clean the assigned exterior surface. Confirm completion with dispatch.', [], '2026-09-06T08:03:00.000Z'); assert.equal(job.assigneeId, 'crew-1'); assert.equal(job.task.includes('Confirm completion'), true); assert.equal('sourceText' in job, false); assert.equal(job.status, 'assigned'); });
